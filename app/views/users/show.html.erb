<table>
  <thead>
    <th>Title</th>
    <th>Opinion</th>
    <th colspan="2"></th>
  </thead>
</table>
  <tbody>
<% @books.each do |book| %>
<% end %>

<h1>New book</h1>
  <%= form_with model: @book do |f| %>
   
    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opinion</h4>
      <%= f.text_area :opinion %>
    <%= f.submit 'Create Book' %>
  <% end %>