<div class="container">
  <div class="row"> 
<div class="col-lg-4">
  <h1>User info</h1>
<table class="table table-striped">
  <tr><th><%= image_tag @user.get_profile_image(50,50) %></th><td> </td></tr>
  <tr><th>name</th><td><%= @user.name %></td></tr>
  <tr><th>introduction</th><td><%= @user.introduction %></td></tr>
</table>

  <h1>New book</h1>
  <% if @book.errors.any? %>
  <%= @book.errors.count %>件のエラーが発生しました
  <ul>
    <% @list.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>

  <%= form_with model: @book do |f| %>

    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opinion</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>
    </div>
    <div class="col-lg-8">
    <div>
      
<table class="table table-striped">
  <thead>
    <tr>
    <th>user_image</th>
    <th>Title</th>
    <th>Opinion</th>
    <th colspan="2"></th>
    </tr>

    <tr>
    <td>
      <%= link_to user_path(@user) do %>
      <%= image_tag @user.get_profile_image(50,50) %>
    <% end %></td>
    <%= form_with model: @book do |f| %>
    <td><%= @post_book.title %></td>
    <td><%= @post_book.body %></td>
    <td><%= link_to "Edit", edit_book_path(@post_book) %></td>
    <td><% if @post_book.user == current_user %><%= link_to "Destroy", book_path(@post_book), method: :delete %><% end %></td>
    <% end %>
    </tr>
  </thead>
  </div>
</table>
</div>
</div>
</div>
  <tbody>

